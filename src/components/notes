<div className="max-w-6xl w-full">
                    {/* Project Title */}
                    <h2 className="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-4 md:mb-6">
                      {project.title}
                    </h2>

                    {/* Project Description */}
                    <p className="text-lg md:text-xl lg:text-2xl text-white/90 mb-6 md:mb-8 max-w-3xl">
                      {project.description}
                    </p>

                    {/* Technology Tags */}
                    {project.technologies && project.technologies.length > 0 && (
                      <div className="flex flex-wrap gap-2 md:gap-3 mb-6 md:mb-8">
                        {project.technologies.map((tech, techIndex) => (
                          <span
                            key={techIndex}
                            className="px-3 py-1 md:px-4 md:py-2 bg-white/20 backdrop-blur-sm rounded-full text-sm md:text-base text-white font-medium"
                          >
                            {tech}
                          </span>
                        ))}
                      </div>
                    )}

                    {/* Project Link */}
                    {project.link && (
                      <a
                        href={project.link}
                        target="_blank"
                        rel="noopener noreferrer"
                        className="inline-block px-6 py-3 md:px-8 md:py-4 bg-white text-gray-900 font-semibold rounded-lg hover:bg-white/90 transition-colors duration-200"
                      >
                        View Project
                      </a>
                    )}
                  </div>